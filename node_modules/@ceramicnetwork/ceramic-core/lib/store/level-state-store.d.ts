import Level from "level-ts";
import { DocState, Doctype } from "@ceramicnetwork/ceramic-common";
import { StateStore } from "./state-store";
import DocID from '@ceramicnetwork/docid';
export declare class LevelStateStore implements StateStore {
    #private;
    private storePath;
    constructor(storePath: string);
    get store(): Level;
    open(): Promise<void>;
    save(document: Doctype): Promise<void>;
    load(docId: DocID): Promise<DocState>;
    exists(docId: DocID): Promise<boolean>;
    remove(docId: DocID): Promise<void>;
    list(docId?: DocID): Promise<string[]>;
    close(): Promise<void>;
}
//# sourceMappingURL=level-state-store.d.ts.map