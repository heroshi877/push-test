import { Pinning } from "./pinning";
import { Context } from "@ceramicnetwork/ceramic-common";
import CID from "cids";
export declare class IpfsPinning implements Pinning {
    #private;
    static designator: string;
    readonly ipfsAddress: string;
    constructor(connectionString: string, context: Context);
    get ipfs(): any;
    open(): Promise<void>;
    close(): Promise<void>;
    pin(cid: CID): Promise<void>;
    unpin(cid: CID): Promise<void>;
}
//# sourceMappingURL=ipfs-pinning.d.ts.map