'use strict';

var bytes = require('../bytes.js');
var base64$1 = require('./base64.js');

const encode = o => Buffer.from(o).toString('base64');
const decode = s => bytes.coerce(Buffer.from(s, 'base64'));
const __browser = false;
var base64 = base64$1({
  encode,
  decode,
  __browser
});

module.exports = base64;
