import { AccountID } from 'caip';
import { BlockchainHandler, BlockchainHandlerOpts } from "../blockchain-handler";
import { LinkProof } from '../utils';
export declare function authenticate(message: string, accountID: AccountID, provider: any): Promise<string>;
export declare function createLink(did: string, accountID: AccountID, provider: any, opts: BlockchainHandlerOpts): Promise<LinkProof>;
export declare function validateLink(proof: LinkProof): Promise<LinkProof | null>;
declare const Handler: BlockchainHandler;
export default Handler;
